# Canonical dependency input for SEC-AUDIT-01 / pip-audit.
#
# These are the non-stdlib packages imported by repository-owned Python code
# outside the Odoo source tree. Odoo itself remains supplied by the pinned
# deployment/runtime image in deployment/runtime/runtime-lock.json.
# The pins are compatible with the Python 3.12 CI baseline and selected for
# current security fixes; the Odoo runtime image remains independently pinned.

cryptography==50.0.0
lxml==6.1.0
python-dateutil==2.9.0.post0
