# Deployment

No Odoo deployment has been created yet.

Future deployment should use Docker Compose, PostgreSQL with managed backups, Nginx reverse proxy, TLS certificates, environment-variable based configuration, and separate DEV, STAGING, and PRODUCTION environments.

Plane is available at `https://plane.cloudpixelstudio.agency`.
